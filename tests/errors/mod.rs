#![allow(clippy::useless_format)] // NOTE(2024.02): Only a performance issue, hence irrelevant for tests

use super::*;

mod comments;
mod escape_str;
mod fs_issues;
mod impl_trait;
mod include_action;
mod stray_token;
mod string_lit;
mod tagged_str;
mod valid_paths;
